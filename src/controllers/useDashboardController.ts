// Redirected to /tickets — kept as stub to avoid TypeScript errors during transition
export const useDashboardController = () => ({ session: null, profile: null, reservations: [], stats: { totalTickets: 0, totalSpent: 0, confirmed: 0 }, savePreferredGenres: () => {} });
